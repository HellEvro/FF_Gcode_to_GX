# Эталон FlashPrint

Положите сюда рабочий двухголовый файл с Creator 3 Pro:

`flashprint_dual.gx`

Затем:

```text
python scripts/calibrate_header.py path\to\наш.gx tests/fixtures/flashprint_dual.gx
```

Файл в git не коммитится (см. `.gitignore`).
